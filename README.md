# Vexarion-GM8

-install pyinstaller in Anaconda
-conda activate sewing_env (to activate the pyinstaller)
--m PyInstaller --version (Check pyinstaller version)
- python -m PyInstaller --onefile --noconsole <python file> (code to build exe file)
- once the exe file was built, place it on api_exes folder
